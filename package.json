{
  "name": "backend-phonebook",
  "version": "1.0.0",
  "description": "phonebook backend",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "git clone git@github.com:kobesani/react-phonebook.git && echo \"cloned react-phonebook repo\"",
    "build:ui": "git clone git@github.com:kobesani/react-phonebook.git && cd ./react-phonebook && npm install && npm run build && cp -r ./build ../build-test"
  },
  "author": "Kobi Rockata",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
